const express = require('express');
const router = express.Router();
const User = require('../models/User');

// Define API routes for creating, fetching, updating, and deleting users

module.exports = router;
